{
    "title": "WHERE Key is calling a numeric operation on a string",
    "input": {
        "WHERE": {
            "LT": {
                "sections_dept": "template string for tests just needs to match type"
            }
        },
        "OPTIONS": {
            "COLUMNS": [
                "sections_title"
            ],
            "ORDER": "sections_title"
        },
        "TRANSFORMATIONS": {
            "GROUP": [
                "sections_title"
            ],
            "APPLY": []
        }
    },
    "errorExpected": true,
    "expected": "InsightError"
}