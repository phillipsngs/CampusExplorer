{
    "title": "querying across multiple datasets should fail",
    "input": {
        "WHERE": {
            "IS": {
                "courses_dept": "cpsc"
            }
        },
        "OPTIONS": {
            "COLUMNS": [
                "courses_id",
                "abcd_avg"
            ],
            "ORDER": "courses_id"
        }
    },
    "errorExpected": true,
    "expected": "InsightError"
}