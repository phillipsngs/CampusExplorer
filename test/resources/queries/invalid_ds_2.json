{
    "title": "an invalid dataset in both columns and order without WHERE should fail",
    "input": {
        "WHERE": {},
        "OPTIONS": {
            "COLUMNS": [
                "invalid_avg"
            ],
            "ORDER": "invalid_avg"
        }
    },
    "errorExpected": true,
    "expected": "InsightError"
}