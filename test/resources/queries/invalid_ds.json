{
    "title": "an invalid dataset should fail",
    "input": {
        "WHERE": {
            "IS": {
                "invalid_dept": "cpsc"
            }
        },
        "OPTIONS": {
            "COLUMNS": [
                "invalid_id",
                "invalid_avg"
            ],
            "ORDER": "invalid_id"
        }
    },
    "errorExpected": true,
    "expected": "InsightError"
}