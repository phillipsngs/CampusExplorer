{
    "title": "querying across multiple datasets should fail 3",
    "input": {
        "WHERE": {
            "IS": {
                "cpsc_dept": "cpsc"
            }
        },
        "OPTIONS": {
            "COLUMNS": [
                "courses_id",
                "courses_avg"
            ],
            "ORDER": "courses_id"
        }
    },
    "errorExpected": true,
    "expected": "InsightError"
}